# BASICS 
1. Data Types - int, float, complex, bool, str ('',"",""" Multiple Lines """)  

2. list[] - Mutable 
            list.append('Value'),
            list[index] = 'Value',
            Nested_list = [1, [2, 3], 4] 
            Ex Nested_list[1][1] = 3

3. Tuple() - Unmutable 
            tuple = (1, 2, 3)
            tuple[1] = 2

4. Sets - Unique Values
          sets = {1,2,3}
          A = {0, 1, 2, 3}
          B = {1, 2, 3, 4, 5, 6}
          # OR = print(A | B) = {0, 1, 2, 3, 4, 5, 6}
          # AND = print(A & B) = {1, 2, 3}
          # Subtract = print(A - B) = {0}
                      print(B - A) = {4, 5, 6} 
          # Unique in Both  = print(A ^ B) = {0, 4, 5, 6}
  
5. Dictionary = {'Key' : 'Value'}
                A = {'name' : 'Omkar', 'Roll_no' : 07}
                A.keys() = (['name', 'Roll_no'])
                A.values() = (['Omkar', 07])
                A.items() = ([('name','Omkar'), ('Roll_no',07)])
                # Index - A['key'] = A['name'] = 'Omkar'
                # Upadte - A.update({dist})
                # Delete - del A['Key']


# Comparsion Operators -> ==, !=, >, <, >=, <=
# Membership Operators -> in , not in

# if-elif-else Statements

# For Loop
# Nested For Loop

# While Loop - break, continue
# While else Loop

# Functions
# Arbitary Functions - def number_args(*args):
                      def number_args(*number):
                      Ex - def number_args(*number):
                              print(number[0]*number[1])

                      Call - number_args(5, 6, 4, 2, 1)
                      Output - 30

# Keyword Arguments - No of Arguments are known
                      Ex - def Square_fun(num, power):
                              print(num**power)
                      Output - 125

                      Call - Square_fun(power = 5, num = 3)
                      Output - 243

# Arbitary Keyword Arguments - No of Arguments are Unknown 


# Converting Data Types

# Automatic File Sorter in File Explorer
# Python Web Scraping
# BeautifulSoup & Request
# Find & Find_all