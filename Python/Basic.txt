# BASICS 
1. Data Types - int, float, complex, bool, str ('',"",""" Multiple Lines """)  

2. list[] - Mutable 
            list.append('Value'),
            list[index] = 'Value',
            Nested_list = [1, [2, 3], 4] 
            Ex Nested_list[1][1] = 3

3. Tuple() - Unmutable 
            tuple = (1, 2, 3)
            tuple[1] = 2

4. Sets - Unique Values
          sets = {1,2,3}
          A = {0, 1, 2, 3}
          B = {1, 2, 3, 4, 5, 6}
          # OR = print(A | B) = {0, 1, 2, 3, 4, 5, 6}
          # AND = print(A & B) = {1, 2, 3}
          # Subtract = print(A - B) = {0}
                      print(B - A) = {4, 5, 6} 
          # Unique in Both  = print(A ^ B) = {0, 4, 5, 6}
  
5. Dictionary = {'Key' : 'Value'}
                A = {'name' : 'Omkar', 'Roll_no' : 07}
                A.keys() = (['name', 'Roll_no'])
                A.values() = (['Omkar', 07])
                A.items() = ([('name','Omkar'), ('Roll_no',07)])
                # Index - A['key'] = A['name'] = 'Omkar'
                # Upadte - A.update({dist})
                # Delete - del A['Key']


# Comparsion Operators -> ==, !=, >, <, >=, <=
# Membership Operators -> in , not in

# if-elif-else Statements

# For Loop
# Nested For Loop

# While Loop